const formatMessage = require('./formatMessage');

getMessagesFromXML = function(str) {
    messages = [];
    // str parsing



    messages.push(formatMessage(str));
}
module.exports = getMessagesFromXML;